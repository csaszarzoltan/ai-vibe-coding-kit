{
  "version": "1.0",
  "tasks": [
    {
      "id": "test-write-001",
      "name": "Write pytest tests for fibonacci function",
      "prompt": "Write pytest test functions for the following fibonacci function. Include tests for: n=0 returning empty list, n=1 returning [0], n=5 returning [0, 1, 1, 2, 3], and at least one edge case. Use pytest style (functions starting with test_).\n\n```python\ndef fibonacci(n):\n    if n <= 0:\n        return []\n    if n == 1:\n        return [0]\n    fib = [0, 1]\n    for i in range(2, n):\n        fib.append(fib[-1] + fib[-2])\n    return fib\n```\n\nReturn only the test code.",
      "expected_answer": "def test_fibonacci_zero():\n    assert fibonacci(0) == []\n\ndef test_fibonacci_one():\n    assert fibonacci(1) == [0]\n\ndef test_fibonacci_five():\n    assert fibonacci(5) == [0, 1, 1, 2, 3]\n\ndef test_fibonacci_negative():\n    assert fibonacci(-1) == []",
      "evaluator": "contains",
      "metadata": {"category": "test-writing", "difficulty": "medium"}
    }
  ]
}
